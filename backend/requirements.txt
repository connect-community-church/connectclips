fastapi==0.115.5
itsdangerous==2.2.0  # signed cookies for SessionMiddleware
uvicorn[standard]==0.32.1
pydantic-settings==2.6.1
python-multipart==0.0.20
faster-whisper==1.1.0
requests==2.33.1
yt-dlp==2026.3.17
anthropic==0.97.0
opencv-python-headless
numpy<2

# YuNet face detection through ONNX Runtime. Default to the CPU build
# everywhere — works on macOS (with CoreML), Windows native, and AMD/CPU
# Linux. install-wsl.sh swaps this for onnxruntime-gpu on Linux+NVIDIA
# boxes via requirements-cuda.txt.
onnxruntime==1.25.1

# whisper.cpp + Metal on Apple Silicon. Compiles from source on install
# (needs Xcode Command Line Tools). Provides Metal-accelerated transcription
# that closes most of the gap with NVIDIA + faster-whisper. On Linux/Windows
# we stick with faster-whisper, so this isn't installed there.
pywhispercpp==1.3.3                  ; sys_platform == 'darwin'
